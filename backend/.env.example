# --- Database ---
DATABASE_URL="postgresql://rantai:rantai@localhost:5433/rantairenteng?schema=public"

# --- App ---
PORT=3001
NODE_ENV=development

# --- Auth ---
JWT_SECRET="change-me-in-production"
JWT_EXPIRES_IN="12h"

# --- On-chain (used by add-web3-relayer; explorer base is read here for tx link derivation) ---
EXPLORER_BASE_URL="https://sepolia.basescan.org"
# Per-cooperative secret salt for on-chain member/appeal hashing (placeholder for web3 change)
COOP_HASH_SALT="replace-with-per-coop-secret-salt"
